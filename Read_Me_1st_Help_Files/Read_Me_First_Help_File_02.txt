Now admin working with admin pages authenticated.

At the moment, I have added some dummy records in database by myselft. But later this would be happend by website.

Check Manage Tutor, it atleast reads the records form database.

Database is bit changed. So remember to re-import database before checking.